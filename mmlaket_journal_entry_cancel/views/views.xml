<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sv_asset_confirm.list">
      <field name="name">sv_asset_confirm list</field>
      <field name="model">sv_asset_confirm.sv_asset_confirm</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sv_asset_confirm.action_window">
      <field name="name">sv_asset_confirm window</field>
      <field name="res_model">sv_asset_confirm.sv_asset_confirm</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sv_asset_confirm.action_server">
      <field name="name">sv_asset_confirm server</field>
      <field name="model_id" ref="model_sv_asset_confirm_sv_asset_confirm"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sv_asset_confirm" id="sv_asset_confirm.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sv_asset_confirm.menu_1" parent="sv_asset_confirm.menu_root"/>
    <menuitem name="Menu 2" id="sv_asset_confirm.menu_2" parent="sv_asset_confirm.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sv_asset_confirm.menu_1_list" parent="sv_asset_confirm.menu_1"
              action="sv_asset_confirm.action_window"/>
    <menuitem name="Server to list" id="sv_asset_confirm" parent="sv_asset_confirm.menu_2"
              action="sv_asset_confirm.action_server"/>
    -->
  </data>
</odoo>